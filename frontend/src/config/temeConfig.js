import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#1b2c4a'
    },
    secondary: {
      main: '#fdfdfe'
    },
    info: {
      main: '#fdfdfe'
    }
  }
})

export default theme
