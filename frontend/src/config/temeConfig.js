import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#1b2c4a'
    },
    text: {
      primary: '#292929',
      secondary: '#fdfdfe'
    }
  }
})

export default theme
